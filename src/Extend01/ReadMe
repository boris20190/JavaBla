1、重写(Override)又叫覆盖，覆写

2、在继承关系中，方法名称一样，参数列表也一样

3、super关键字是把父类中的某一个内容直接拿来用

4、在使用父类功能，但子类又增加新功能时，用覆盖

5、继承关系中构造方法访问顺序：
   先父类后子类
6、子类构造方法中右默认的super()调用，什么都不写会默认调用

7、super父类调用只能是第一个语句，子类只能调用一次super

####################################################################
注意事项：
1、必须保证父子类之间方法名称相同，参数列表也相同

2、@override 写在方法前面，用来【检测】是不是有效的覆盖重写 （注解）

3、子类值的范围必须 <= 父类方法的返回值范围————父类是int，子类就不能是long
   Object大于一切，当然不能比如父类是String，子类是Object

4、子类方法权限必须 >= 父类方法权限
   public > protected > (default)什么都不写 > private

